namespace SharedDomain.Defaults
{
    public class Roles
    {
        public const string SuperAdmin = nameof(AccountType.SuperAdmin);
        public const string Admin = nameof(AccountType.Admin);
        public const string Manager = nameof(AccountType.Manager);
        public const string Member = nameof(AccountType.Member);

    }
}